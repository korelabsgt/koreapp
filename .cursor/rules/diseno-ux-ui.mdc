---
description: Diseño UX/UI zinc — tema, botones, modales, toastify, SweetAlert, gráficos, animaciones, offline
alwaysApply: true
---

# Diseño UX/UI

## Tema

- Claro: página blanca; cards `zinc-50`; header/layout `zinc-100`.
- Oscuro: página `zinc-900`; cards/componentes `zinc-800`.

## Prohibiciones globales

- Botones: sin borde visible, sin sombra; `cursor-pointer`.
- Formularios flotantes: solo `ModalShell`. Prohibido `Dialog` de shadcn.
- Feedback: solo `react-toastify`; un `ToastContainer` global.
- SweetAlert solo **fuera** de `ModalShell`; dentro → `ModalConfirmDelete`.
- Listas dinámicas: `AnimatePresence` + `motion` con `layout`.
- Conexión: un solo `OfflineBanner` global; respetar `--banner-height`.
- Menús flotantes (`DropdownMenu`, popovers, menús de 3 puntos): **fondo opaco sólido**. Prohibido dejar solo `bg-popover` si se ve transparente. Usar `bg-white dark:bg-zinc-900`, `opacity-100` y `z` alto (p. ej. `z-[200]`) para que no se transparente contenido detrás (paginación, filas, etc.).

## Skills UI (cargar según necesidad)

| Skill | Cuándo |
|-------|--------|
| `ui-tema-botones` | Superficies zinc, botones |
| `ui-modales` | ModalShell, formularios flotantes |
| `ui-toastify` | toast.success/error/warn |
| `ui-sweetalert` | Confirmaciones fuera de modal |
| `ui-graficos` | Donas, barras, anillos de progreso, KPIs |
| `ui-animaciones-listas` | Filas/tarjetas que se agregan o quitan |
| `ui-tablas` | Tablas con toolbar, paginación, lista/tarjetas, export |
| `ui-offline-banner` | Layouts sticky, estado offline/lento |

Todos en `.cursor/skills/`.
